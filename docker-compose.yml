version: '3.4'

services:
  api:
    build:
      context: .
      target: production
    # image: registry.heroku.com/safe-badlands-10026/web
    # volumes:
    #   - ./:/usr/src/app
    #   - /usr/src/app/node_modules
    env_file: .env
    ports:
      - 3000:3000
    # command: npm run dev
